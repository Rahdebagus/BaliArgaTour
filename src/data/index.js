// Barrel for shared (cross-feature) data. Domain data now lives in its feature
// module — e.g. import { packages } from '@/features/packages'.
export { company, whatsappLink } from './company';
export { testimonials } from './testimonials';
