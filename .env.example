# Copy to `.env` and fill in as needed.

# Base URL of a future REST/GraphQL backend (docs/12_FUTURE_BACKEND.md).
# When set, the service layer switches from dummy data to real fetch() calls.
# Leave empty to keep using the built-in dummy data.
VITE_API_BASE_URL=
